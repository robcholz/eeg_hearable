def synthesize_soundscape(bg_path: str, event_paths: list[str], out_path: str):
    """
    Minimal interface:
      - bg_path: path to a background audio file (will be looped/cropped to match length)
      - event_paths: list of event audio files (randomly placed and mixed into background)
      - out_path: output wav path
    """
    # todo
    pass
